/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { health } from '../fn/health/health';
import { Health$Params } from '../fn/health/health';
import { HealthResponse } from '../models/health-response';


/**
 * Operational health checks
 */
@Injectable({ providedIn: 'root' })
export class HealthService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `health()` */
  static readonly HealthPath = '/api/health';

  /**
   * Health check.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `health()` instead.
   *
   * This method doesn't expect any request body.
   */
  health$Response(params?: Health$Params, context?: HttpContext): Observable<StrictHttpResponse<HealthResponse>> {
    const obs = health(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Health check.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `health$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  health(params?: Health$Params, context?: HttpContext): Observable<HealthResponse> {
    const resp = this.health$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<HealthResponse>): HealthResponse => r.body)
    );
  }

}
