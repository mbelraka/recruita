/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ParseActionCommandRequest } from '../../models/parse-action-command-request';
import { ParseActionResponse } from '../../models/parse-action-response';

export interface Parse$Params {
      body: ParseActionCommandRequest
}

export function parse(http: HttpClient, rootUrl: string, params: Parse$Params, context?: HttpContext): Observable<StrictHttpResponse<ParseActionResponse>> {
  const rb = new RequestBuilder(rootUrl, parse.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ParseActionResponse>;
    })
  );
}

parse.PATH = '/api/action/parse';
