/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApplicantDto } from '../../models/applicant-dto';
import { SaveApplicantRequestDto } from '../../models/save-applicant-request-dto';

export interface CreateApplicant$Params {
      body: SaveApplicantRequestDto
}

export function createApplicant(http: HttpClient, rootUrl: string, params: CreateApplicant$Params, context?: HttpContext): Observable<StrictHttpResponse<ApplicantDto>> {
  const rb = new RequestBuilder(rootUrl, createApplicant.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApplicantDto>;
    })
  );
}

createApplicant.PATH = '/api/applicants';
