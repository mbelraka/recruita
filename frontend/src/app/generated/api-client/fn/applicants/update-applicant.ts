/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApplicantDto } from '../../models/applicant-dto';
import { SaveApplicantRequestDto } from '../../models/save-applicant-request-dto';

export interface UpdateApplicant$Params {
  id: string;
      body: SaveApplicantRequestDto
}

export function updateApplicant(http: HttpClient, rootUrl: string, params: UpdateApplicant$Params, context?: HttpContext): Observable<StrictHttpResponse<ApplicantDto>> {
  const rb = new RequestBuilder(rootUrl, updateApplicant.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApplicantDto>;
    })
  );
}

updateApplicant.PATH = '/api/applicants/{id}';
