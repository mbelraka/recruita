/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApplicantDto } from '../../models/applicant-dto';

export interface GetApplicant$Params {
  id: string;
}

export function getApplicant(http: HttpClient, rootUrl: string, params: GetApplicant$Params, context?: HttpContext): Observable<StrictHttpResponse<ApplicantDto>> {
  const rb = new RequestBuilder(rootUrl, getApplicant.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApplicantDto>;
    })
  );
}

getApplicant.PATH = '/api/applicants/{id}';
