import type { ApplicantSummaryDto } from '../../../generated/api/types';

/** JSON list projection from Spring `ApplicantSummaryDto` (no notes). */
export type ApplicantApiSummaryRecord = ApplicantSummaryDto;
