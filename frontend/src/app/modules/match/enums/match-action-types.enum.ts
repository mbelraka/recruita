export enum MatchActionTypes {
  SetJobDescription = '[Match] Set Job Description',
  ResetState = '[Match] Reset State',
  InvalidateResults = '[Match] Invalidate Results',
  EvaluateCandidates = '[Match] Evaluate Candidates',
  EvaluateCandidatesSuccess = '[Match] Evaluate Candidates Success',
  EvaluateCandidatesFailure = '[Match] Evaluate Candidates Failure',
}
