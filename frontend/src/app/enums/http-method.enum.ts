/** HTTP methods used by CSRF and API clients. */
export enum HttpMethod {
  Get = 'GET',
  Head = 'HEAD',
  Options = 'OPTIONS',
  Post = 'POST',
  Put = 'PUT',
  Patch = 'PATCH',
  Delete = 'DELETE',
}
