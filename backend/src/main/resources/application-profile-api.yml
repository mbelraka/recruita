recruita:
  profile-api:
    admin-id: admin
    messages:
      id-required: Profile id is required.
      last-language-required: Last language is required.
      id-mismatch: Profile id in the request body must match the path id.
      not-found: Profile not found.
      already-exists: A profile with this id already exists.
